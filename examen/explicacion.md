----
# Ejecucion de cada script

---

El script se ejecuta en un motor de base de datos como MySQl u otro esto incluye que la base de datos este creada con las tablas que esta misma tenga 


----

La subconsulta y el view se visualiza despues de la ejecucion del script en donde se encuentre  estas funcionalidades 

----

Con el trigger es un caso distinto ya que se ejecuta pero los resultos se ven despues en el caso del trigger que se creo se ejecutara des pues de actulizar la tabla productos 